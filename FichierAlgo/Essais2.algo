ALGORITHME OUAIAIAIAIIAIAIAI
constante:
	STRING   <-- "ma chaine"
	DOUBLE   <-- 25.01
variable:
	x: entier
	y: chaine de caractères
DEBUT
	ecrire ( "Salut", " Paul" © "comment ça va" );
	lire ( x, y )
	
	ecrire ( STRING )
	ecrire ( DOUBLE )
	
	ecrire ( x )
	ecrire ( y )

FIN