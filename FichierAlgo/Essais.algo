ALGORITHME Essais
constante:
	STRING   <-- "ma chaine"
	DOUBLE   <-- 25.01
	ENTIER   <-- 456
	BOOLEAN  <-- false
	CHAR     <-- 'C'
variable:
    t, u,v: chaine de caractères
	w: caractère
	x: entier
	y: réel
	z: booléen
DEBUT
	lire ( u )
	u <-- "Yo"
	v <-- "Salut er"
	w <-- 'C'
	x <-- 2 ^ 3 + 4/(5 + 6)
	y <-- 2 ^ 3 + 4/(5 + 6)
	z <-- false

	ecrire(STRING)
	ecrire(DOUBLE)
	ecrire(ENTIER)
	ecrire(BOOLEAN)
	ecrire(CHAR)

	ecrire(v)
	ecrire(w)
	ecrire(x)
	ecrire(u)
	ecrire(y)
	ecrire(z)
FIN